# メモ

問題の根っこは「等方性」という縛り方だと思います。スジは本質的に異方性の構造なので、物理的に正しい強調処理（スジ方向に平均してSNRを稼ぎ、直交方向でベースを差し引く）は必ず異方的になります。一方で縮小は等方操作なので縛りをすり抜けてしまう。つまり禁止したいのは異方性ではなく「解像度と次元を落とすこと」なので、制約をこう置き換えるのが良さそうです。

- 出力は入力と同じ縦横ピクセル数（少なくとも横方向の解像度は維持）。1次元化・射影・リサイズは一切禁止
- 各出力画素は入力の局所近傍のみから計算する（シフト不変な局所演算に限定）
- この2つを守れば異方性フィルタは解禁

パイプラインとしては、エージェントがやった縦積算プロファイルは方向性自体は正しくて、極端すぎただけです。中庸をとると、まず縦1×Nの箱型平均（Nは数十px程度）でノイズだけを1/√Nに落とし、スジはそのまま残します。次に幅の大きい横方向メディアン（スジ幅よりずっと広い、100px級）でベース変動を推定して差し引きます。メディアンなら1〜2pxのスジがベース側に混入しません。残差をパーセンタイル基準（1%〜99%点など）でコントラストストレッチして中間グレー±に展開すれば、人間にも淡いスジが見える画になります。

もうひとつ重要なのがVLM側の縮小です。VLMは入力画像をトークン予算に合わせて内部でリサイズするのが普通なので、1〜2pxのスジはコントラストを上げてもリサイズの段階で消えます。対策は、横方向に数pxのmax/minフィルタでスジを太らせるか、原寸のままタイル分割して渡すことです。逆に縦方向だけの縮小は平均化と同じ効果なのでむしろ有利で、これも異方性を解禁する理由になります。

「見やすく」を8Bに聞かせるには、美的な指示を数値目標に変換するのが現実的だと思います。欠陥なしのスキャンに振幅既知の疑似スジを注入した評価セットを作り、VLM入力解像度まで縮小した後のスジ領域のCNRをスコアにするか、VLM自身にブラインドで「スジは何本、どこにあるか」を答えさせて正解率を測る。コーダーにはこのスコアの最大化だけを課し、制約は操作のホワイトリストではなく上の不変条件で与える形です。小さいモデルは意図では動かず、機械的なルールと数値フィードバックでなら動くので、良いパイプラインの実例を1つfew-shotで見せておくとさらに安定すると思います。

作成日時: 2026-09-01 15:04:17