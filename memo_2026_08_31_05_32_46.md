# メモ

アイデアを整理します。EDIDのようなプロファイル交換の枠組みをVLMの認識特性に持ち込む発想ですね。以前のブラーによるキャリブレーション案の上位概念化とも言えます。

**課題**

- VLMは入力画像に対して内部でリサイズ・正規化・トークン化を行うため、印刷物の微細な欠陥（低コントラストのスジむら、微小な段差など)の情報が入力段階で失われることがある
- 識別可能な最小欠陥サイズ、コントラスト感度、階調分解能といった「視覚特性」はVLMのモデルやバージョンごとに異なるが、送信側はそれを知らないまま一律の条件で画像を送っており、欠陥の見落としや検出精度の不安定化が生じる
- モデル更新のたびに特性が変わりうるため、固定的な前処理では追従できない

**解決手段**

- 画像入力に先立ち、検査装置がVLM側（またはプロファイル管理部）に視覚特性プロファイルを要求し、受信する
- プロファイルの内容例：視認性が保持できる実効画像サイズ、コントラスト感度（空間周波数ごとの識別閾値）、ダイナミックレンジ、色分解能
- 受信したプロファイルに基づき、画像を調整して送信する。具体的には、欠陥サイズが識別閾値を上回るような分割・拡大、コントラスト強調、階調のトーンマッピングなど
- プロファイルの生成方法として、(a) モデル提供側が事前測定して付与する宣言的プロファイル、(b) テストチャート画像を入力し応答から測定するキャリブレーション部を装置側に持つ構成、の両方をクレームに含めておくと幅が出ます

**効果**

- モデル差・バージョン差を吸収し、欠陥検出精度が安定する
- 必要以上の高解像度・多分割送信を避けられ、トークン数と処理コストが最適化される
- モデル更新時にプロファイル再取得だけで再適合でき、前処理の再設計が不要になる

**正直な懸念点**

一点、実施可能性の面で指摘しておきます。現在のVLMは自分の視覚特性を正確に自己申告できません（内省が不正確なため、「あなたの識別可能な最小コントラストは？」と聞いても信頼できる答えは返りません）。そのため発明の中核は「VLMに聞く」ではなく、「測定またはメタデータとして保持されたプロファイルを要求・応答するプロトコル」として構成する方が堅牢です。上記(b)のキャリブレーション部を主実施形態に置くのが安全だと思います。

また、ディスプレイのEDIDやICCプロファイル交換と構造が似ているため、先行技術調査ではその系統との差異（機械学習モデルの認識特性という対象の新規性、測定による動的取得）を明確にする必要があります。ここは弁理士との確認をお勧めします。

作成日時: 2026-08-31 14:32:39