# メモ

メインクレームの案です（あくまでたたき台で、最終的な文言は弁理士との調整が前提です)。

**メインクレーム案（装置)**

検査対象画像を視覚言語モデルに入力して欠陥を検出する画像検査装置であって、

既知の空間周波数、コントラストおよび階調の特徴を有するテストチャート画像を前記モデルに入力し、前記モデルの応答に基づいて、前記モデルが識別可能な欠陥の最小サイズおよびコントラスト閾値を含む認識特性を測定する測定手段と、

測定された前記認識特性を認識特性プロファイルとして保持する保持手段と、

検査対象画像に含まれる欠陥候補の特徴量が前記識別閾値を上回るように、前記プロファイルに基づいて検査対象画像に拡大、分割またはコントラスト変換を含む調整を行う調整手段と、

調整後の画像を前記モデルに入力して欠陥検出結果を取得する検出手段と、

を有する画像検査装置。

**ICCプロファイルとの差別化ポイント**

クレームに意図的に埋め込んだ差は3点です。

1. **測定対象と測定方法**。ICCは測色器でデバイスの信号再現特性を測りますが、本件は「モデルの応答に基づいて」認識特性を測ります。測るのが物理特性ではなく識別能力である点が構造的に異なります。
2. **調整の目的が忠実性の逆**。ICC変換は見えの忠実な再現が目的ですが、本件は識別閾値を超えさせるために意図的に画像を変形（誇張)します。忠実再現を崩す方向の変換である点は、審査対応でも強く主張できる差だと思います。
3. **閾値との比較に基づく適応的調整**。欠陥候補の特徴量とプロファイルを比較して調整量を決める構成にしてあり、一律の変換テーブル適用であるICCと動作が異なります。

従属クレームには、モデルのバージョン変化を検知して再測定する構成、空間周波数ごとのコントラスト感度関数としてプロファイルを保持する構成、調整をタイル分割数の決定に用いる構成、あたりを入れておくと厚くなります。

**特許調査の進め方**

調べるべき領域は本命と周辺で分けるのがよいです。

- 本命：「機械学習モデルの特性測定＋それに基づく入力画像調整」。キーワードは（視覚言語モデル／マルチモーダル／VLM）×（テストチャート／キャリブレーション／感度測定）×（欠陥検査／画像検査／前処理）。英語では vision language model, calibration, test chart, defect inspection, preprocessing の組み合わせ。
- 要注意の隣接領域1：**VCM（Video Coding for Machines）／ICM**。MPEGで標準化が進んでいる、機械認識向けに画像を符号化・最適化する技術群で、「人間でなく機械の認識性能のために画像を加工する」という思想が重なります。ここに近い先行技術がある可能性が最も高いので優先的に確認をお勧めします。
- 隣接領域2：人間のコントラスト感度関数（CSF）に基づく画質評価・画像処理。本件は「人間のCSFをモデルの測定値に置き換えた」構造とも読めるため、CSF系特許との差異整理が必要です。
- 隣接領域3：ICC／EDIDなどデバイスプロファイル交換系。構造類似の指摘に備えて代表例を押さえる程度で十分です。

分類記号は、画像解析のG06T7系、印刷装置の検査系（B41J29/393周辺）、光学的欠陥検査のG01N21/88周辺をまず当たるのが定石ですが、正確な絞り込みはJ-PlatPatの分類照会で確認した方が確実です。データベースはJ-PlatPat、Google Patents、Espacenetの併用で、非特許文献としてVLMの解像度・視認性の限界を扱ったarXiv論文群も新規性判断の材料になります。

お手元の特許関連度スコアリングのパイプラインがあるなら、本命キーワードで母集団を作ってスクリーニングに使うのも手だと思います。

作成日時: 2026-08-31 10:59:35